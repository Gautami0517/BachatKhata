/**
 * Notification channel types for current + future features.
 * Only EXPIRY is implemented today.
 */
export enum NotificationType {
  EXPIRY = 'EXPIRY',
  RECOMMENDATION = 'RECOMMENDATION',
  SYSTEM = 'SYSTEM',
}
